<%= form_for project do |f| %>
  <%= link_to project.name, project, remote: true %>
  </li><%= link_to "(remove)", project, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>

